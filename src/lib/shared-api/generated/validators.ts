/**
 * Data Version Control (DVC) v5.0 - Generated Validators
 *
 * Auto-generated from TypeSpec models with validation decorators
 * Do not edit this file directly
 *
 * Source: typespec/ folder
 * Generated: 2026-04-03T06:42:24.879Z
 */

// ============================================================================
// TYPES
// ============================================================================

export interface ValidationResult {
  valid: boolean;
  errors: string[];
}

export interface FieldValidationError {
  field: string;
  message: string;
}

// ============================================================================
// VALIDATORS
// ============================================================================

/**
 * Validator for PaginationInput
 * 
 * Pagination request parameters
 */
export function validatePaginationInput(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate pageSize (optional)
  if (obj.pageSize !== undefined && obj.pageSize !== null) {
    if (typeof obj.pageSize === "number" && obj.pageSize < 1) {
      errors.push(`${fieldPrefix}pageSize must be at least 1`);
    }
    if (typeof obj.pageSize === "number" && obj.pageSize > 1000) {
      errors.push(`${fieldPrefix}pageSize must be at most 1000`);
    }
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for TableMetadata
 * 
 * Metadata about table creation, updates, and state
 */
export function validateTableMetadata(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate snapshotId (optional)
  if (obj.snapshotId !== undefined && obj.snapshotId !== null) {
    if (typeof obj.snapshotId === "string" && !/^[0-9]+$/.test(obj.snapshotId)) {
      errors.push(`${fieldPrefix}snapshotId must match pattern: ^[0-9]+$`);
    }
  }

  // Validate s3Location (optional)
  if (obj.s3Location !== undefined && obj.s3Location !== null) {
    if (typeof obj.s3Location === "string" && !/^s3:\/\/[a-z0-9][a-z0-9.-]*[a-z0-9](\/.*)?$/.test(obj.s3Location)) {
      errors.push(`${fieldPrefix}s3Location must match pattern: ^s3://[a-z0-9][a-z0-9.-]*[a-z0-9](/.*)?$`);
    }
  }

  // Validate metadataLocation (optional)
  if (obj.metadataLocation !== undefined && obj.metadataLocation !== null) {
    if (typeof obj.metadataLocation === "string" && !/^s3:\/\/[a-z0-9][a-z0-9.-]*[a-z0-9](\/.*)?$/.test(obj.metadataLocation)) {
      errors.push(`${fieldPrefix}metadataLocation must match pattern: ^s3://[a-z0-9][a-z0-9.-]*[a-z0-9](/.*)?$`);
    }
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for IcebergMetadata
 * 
 * Iceberg-specific metadata
 */
export function validateIcebergMetadata(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate currentSnapshotId (optional)
  if (obj.currentSnapshotId !== undefined && obj.currentSnapshotId !== null) {
    if (typeof obj.currentSnapshotId === "string" && !/^[0-9]+$/.test(obj.currentSnapshotId)) {
      errors.push(`${fieldPrefix}currentSnapshotId must match pattern: ^[0-9]+$`);
    }
  }

  // Validate metadataFileLocation (optional)
  if (obj.metadataFileLocation !== undefined && obj.metadataFileLocation !== null) {
    if (typeof obj.metadataFileLocation === "string" && !/^s3:\/\/[a-z0-9][a-z0-9.-]*[a-z0-9](\/.*)?$/.test(obj.metadataFileLocation)) {
      errors.push(`${fieldPrefix}metadataFileLocation must match pattern: ^s3://[a-z0-9][a-z0-9.-]*[a-z0-9](/.*)?$`);
    }
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for ActualDependency
 * 
 * System-detected dependency from lineage analysis
 */
export function validateActualDependency(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate confidence (optional)
  if (obj.confidence !== undefined && obj.confidence !== null) {
    if (typeof obj.confidence === "number" && obj.confidence < 0) {
      errors.push(`${fieldPrefix}confidence must be at least 0`);
    }
    if (typeof obj.confidence === "number" && obj.confidence > 1) {
      errors.push(`${fieldPrefix}confidence must be at most 1`);
    }
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for CreateTableRequest
 */
export function validateCreateTableRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate table
  if (obj.table === undefined || obj.table === null) {
    errors.push(`${fieldPrefix}table is required`);
  }
  if (typeof obj.table === "string" && obj.table.length < 3) {
    errors.push(`${fieldPrefix}table must be at least 3 characters`);
  }
  if (typeof obj.table === "string" && obj.table.length > 64) {
    errors.push(`${fieldPrefix}table must be at most 64 characters`);
  }
  if (typeof obj.table === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.table)) {
    errors.push(`${fieldPrefix}table must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for CreateTableResponse
 */
export function validateCreateTableResponse(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate tableName
  if (obj.tableName === undefined || obj.tableName === null) {
    errors.push(`${fieldPrefix}tableName is required`);
  }
  if (typeof obj.tableName === "string" && obj.tableName.length < 3) {
    errors.push(`${fieldPrefix}tableName must be at least 3 characters`);
  }
  if (typeof obj.tableName === "string" && obj.tableName.length > 64) {
    errors.push(`${fieldPrefix}tableName must be at most 64 characters`);
  }
  if (typeof obj.tableName === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.tableName)) {
    errors.push(`${fieldPrefix}tableName must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for TableCatalogEntry
 * 
 * Table catalog entry for list views with lineage information
- Includes basic table info (description available via getTable)
- Includes upstream dependencies in compact format for building catalog trees
- Excludes heavy data like full schema definition
- Excludes downstream dependencies (use GetTableDependents API instead)
 */
export function validateTableCatalogEntry(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate tableName
  if (obj.tableName === undefined || obj.tableName === null) {
    errors.push(`${fieldPrefix}tableName is required`);
  }
  if (typeof obj.tableName === "string" && obj.tableName.length < 3) {
    errors.push(`${fieldPrefix}tableName must be at least 3 characters`);
  }
  if (typeof obj.tableName === "string" && obj.tableName.length > 64) {
    errors.push(`${fieldPrefix}tableName must be at most 64 characters`);
  }
  if (typeof obj.tableName === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.tableName)) {
    errors.push(`${fieldPrefix}tableName must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for ImportTableRequest
 */
export function validateImportTableRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate table
  if (obj.table === undefined || obj.table === null) {
    errors.push(`${fieldPrefix}table is required`);
  }
  if (typeof obj.table === "string" && obj.table.length < 3) {
    errors.push(`${fieldPrefix}table must be at least 3 characters`);
  }
  if (typeof obj.table === "string" && obj.table.length > 64) {
    errors.push(`${fieldPrefix}table must be at most 64 characters`);
  }
  if (typeof obj.table === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.table)) {
    errors.push(`${fieldPrefix}table must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for ListActivityEventsRequest
 * 
 * Request to list events for the Activity Feed

GET /events
 */
export function validateListActivityEventsRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate limit (optional)
  if (obj.limit !== undefined && obj.limit !== null) {
    if (typeof obj.limit === "number" && obj.limit > 100) {
      errors.push(`${fieldPrefix}limit must be at most 100`);
    }
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for GetTableHistoryRequest
 * 
 * Get table history request
 */
export function validateGetTableHistoryRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate table
  if (obj.table === undefined || obj.table === null) {
    errors.push(`${fieldPrefix}table is required`);
  }
  if (typeof obj.table === "string" && obj.table.length < 3) {
    errors.push(`${fieldPrefix}table must be at least 3 characters`);
  }
  if (typeof obj.table === "string" && obj.table.length > 64) {
    errors.push(`${fieldPrefix}table must be at most 64 characters`);
  }
  if (typeof obj.table === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.table)) {
    errors.push(`${fieldPrefix}table must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for RollbackToMinorVersionRequest
 * 
 * Rollback to minor version request
 */
export function validateRollbackToMinorVersionRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate table
  if (obj.table === undefined || obj.table === null) {
    errors.push(`${fieldPrefix}table is required`);
  }
  if (typeof obj.table === "string" && obj.table.length < 3) {
    errors.push(`${fieldPrefix}table must be at least 3 characters`);
  }
  if (typeof obj.table === "string" && obj.table.length > 64) {
    errors.push(`${fieldPrefix}table must be at most 64 characters`);
  }
  if (typeof obj.table === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.table)) {
    errors.push(`${fieldPrefix}table must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate branch
  if (obj.branch === undefined || obj.branch === null) {
    errors.push(`${fieldPrefix}branch is required`);
  }
  if (typeof obj.branch === "string" && !/^v[0-9]+\\.(main|beta|feature-[a-z0-9]+(-[a-z0-9]+)*)$/.test(obj.branch)) {
    errors.push(`${fieldPrefix}branch must match pattern: ^v[0-9]+\.(main|beta|feature-[a-z0-9]+(-[a-z0-9]+)*)$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

/**
 * Validator for CompareVersionsRequest
 * 
 * Compare versions request
 */
export function validateCompareVersionsRequest(
  obj: any,
  fieldPrefix = ""
): ValidationResult {
  const errors: string[] = [];

  if (!obj || typeof obj !== "object") {
    return {
      valid: false,
      errors: ["Input must be an object"],
    };
  }

  // Validate database
  if (obj.database === undefined || obj.database === null) {
    errors.push(`${fieldPrefix}database is required`);
  }
  if (typeof obj.database === "string" && obj.database.length < 3) {
    errors.push(`${fieldPrefix}database must be at least 3 characters`);
  }
  if (typeof obj.database === "string" && obj.database.length > 64) {
    errors.push(`${fieldPrefix}database must be at most 64 characters`);
  }
  if (typeof obj.database === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.database)) {
    errors.push(`${fieldPrefix}database must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  // Validate table
  if (obj.table === undefined || obj.table === null) {
    errors.push(`${fieldPrefix}table is required`);
  }
  if (typeof obj.table === "string" && obj.table.length < 3) {
    errors.push(`${fieldPrefix}table must be at least 3 characters`);
  }
  if (typeof obj.table === "string" && obj.table.length > 64) {
    errors.push(`${fieldPrefix}table must be at most 64 characters`);
  }
  if (typeof obj.table === "string" && !/^[a-z][a-z0-9_]*[a-z0-9]$/.test(obj.table)) {
    errors.push(`${fieldPrefix}table must match pattern: ^[a-z][a-z0-9_]*[a-z0-9]$`);
  }

  return {
    valid: errors.length === 0,
    errors,
  };
}

// ============================================================================
// SUMMARY: Generated 12 validators
// ============================================================================
